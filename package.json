{
  "name": "genie-smx",
  "description": "An SMX file format parser, used by the genie engine.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "version": "1.0.2",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.36",
    "typescript": "^4.7.2"
  },
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "tsc"
  },
  "dependencies": {
    "awestruct": "^1.2.5",
    "buffer": "^6.0.3",
    "jascpal": "^0.1.4"
  }
}
